{
  "name": "mock_api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --experimental-modules --es-module-specifier-resolution=node src/server",
    "heroku": "node --experimental-modules --es-module-specifier-resolution=node src/server"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3",
    "json-server": "^0.17.0"
  },
  "devDependencies": {
    "sucrase": "^3.20.3",
    "nodemon": "^2.0.15"
  }
}
